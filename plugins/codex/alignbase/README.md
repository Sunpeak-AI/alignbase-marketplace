# Alignbase for Codex

Install the plugin, add the Alignbase MCP server with the OAuth Client ID from Alignbase, sign in, run `codex`, approve the Alignbase hook, then start a new session to confirm access to Alignbase context.
